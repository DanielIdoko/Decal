import { Router } from "express";
// import { signOut, signUp, signIn } from "../controllers/auth.controller.js";


const progressRoute = Router();

progressRoute.post("/", )
progressRoute.post("/weekly", )
progressRoute.post("/daily", )

progressRoute.get("/", )
progressRoute.get("/weekly", )
progressRoute.get("/daily", )

export default progressRoute;