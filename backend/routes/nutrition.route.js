import { Router } from "express";
// import { signOut, signUp, signIn } from "../controllers/auth.controller.js";


const nutritionRoute = Router();

nutritionRoute.post("/create-diet", )
nutritionRoute.get("/", )
nutritionRoute.get("/:id", )
nutritionRoute.put("/:id", )
nutritionRoute.delete("/:id", )

export default nutritionRoute;